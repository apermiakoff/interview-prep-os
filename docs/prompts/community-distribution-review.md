# Community distribution review prompt

Review this private-alpha community distribution as an adversarial first-time self-hoster.

1. Render `compose.community.yaml` with AI off/on. Identify public bindings, host-specific paths, source/legacy mounts, secret leakage, excess capabilities, shared core/worker data, missing health checks, and restart loops.
2. Boot unique empty volumes and prove that only generic catalog/skills are seeded—never assignments, attempts, reviews, mastery, profile snapshots, or personal strings.
3. Exercise setup in noninteractive mode for Ollama, OpenAI, Anthropic, and OpenAI-compatible providers. Confirm URL/model/budget validation, exact Docker command, key masking, and mode `0600`.
4. Send hostile Origin and Referer writes to every core and AI endpoint class; verify same-origin, configured private origins, and documented headerless CLI behavior.
5. Back up and restore both databases. Corrupt each archive member/manifest, test overwrite refusal, inspect the zip for keys, and run `quick_check`.
6. Search tracked files, rendered configuration, image layers, and full history for secrets, personal paths/names, tailnet hosts, and unauthorized Outtalent material.
7. Run all quality gates and browser smoke. Report findings by severity with exact reproduction and do not deploy, push, call providers, or inspect personal data.
